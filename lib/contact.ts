/**
 * Public inbox shown on the contact page and mailto links.
 * Set `NEXT_PUBLIC_CONTACT_EMAIL` to override (optional).
 */
export const CONTACT_EMAIL =
  process.env.NEXT_PUBLIC_CONTACT_EMAIL?.trim() || "gbnguyenw@gmail.com";
